<Page
    x:Class="ShadowPlayReminderWidget.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ShadowPlayReminderWidget"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Padding="24" RowSpacing="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="12">
            <TextBlock Text="ShadowPlay Reminder" FontSize="24" FontWeight="SemiBold" />
            <Border x:Name="StatusBorder" CornerRadius="8" Padding="16" Background="#222831">
                <StackPanel Spacing="4">
                    <TextBlock Text="ShadowPlay status" Opacity="0.7" />
                    <TextBlock x:Name="StatusText" Text="Checking ShadowPlay activity…" TextWrapping="WrapWholeWords" FontSize="16" />
                </StackPanel>
            </Border>
            <TextBlock x:Name="LastCheckedText" Text="Waiting for first check…" Opacity="0.7" />
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="16" VerticalAlignment="Center">
            <ToggleSwitch x:Name="NotificationsToggle" Header="Game Bar reminders" IsOn="True" />
            <Button x:Name="CheckNowButton" Content="Check now" />
        </StackPanel>

        <TextBlock Grid.Row="2"
                   TextWrapping="WrapWholeWords"
                   Opacity="0.7"
                   Text="ShadowPlay stores temporary files named Sha*.tmp in its working folder. This widget monitors that folder using broad file system access. When the Game Bar target tracker reports a game without recent ShadowPlay activity it uses the Game Bar notification SDK to remind you to re-enable recording." />
    </Grid>
</Page>
